# Image Viewer

A simple C program that reads and displays PPM (P6) images using SDL2.

## Features

- Loads and displays PPM (P6) images
- Uses SDL2 for rendering

## Requirements

- **SDL2** library

## Building

1. Install SDL2 (e.g., `sudo apt-get install libsdl2-dev`).
2. Compile:

   ```bash
   make
   make run
   make clean
